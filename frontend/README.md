# Frontend

Not scaffolded yet — this is a Phase 6 task (see Implementation_Development_Plan.md).

When you get there:

```
npx create-next-app@latest . --typescript --tailwind --app
```

Then build the streaming chat interface against the backend's `/health` and
(later) `/chat` endpoints.
